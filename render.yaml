services:
  - type: web
    name: chamber-sensor-dashboard
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn chamber_dashboard:server --workers 1 --threads 2 --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
